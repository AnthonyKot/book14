# Research copies are not publication assets. Free access does not establish reuse rights.
sources/**/*.pdf
sources/**/*.html
sources/**/*.txt
sources/**/*.rtf
sources/**/*.zip
sources/**/*.epub


# Helper-model run logs are scratch, not evidence.
extract/*.log
extract/.*.done
